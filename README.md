# GoharsLab

Static luxury cosmetics website deployed on Netlify.

## Netlify

- Publish directory: `outputs`
- Functions directory: `netlify/functions`
- Main domain: `goharslab.com`
- Redirect domain: `www.goharslab.com`

The build command is intentionally empty because the site is already built as static files in `outputs`.
